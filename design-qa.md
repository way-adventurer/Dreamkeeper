# 守梦 UI 设计验收

## 结论

final result: passed

本轮实现采用已选定的 Product Design 方案 1：面向程序员的深色运行控制台。首页和配置页均完成视觉与交互核验，没有发现 P0、P1 或 P2 级问题。

## 视觉依据

- 设计参考：Product Design 方案 1（深色运行控制台）
- 本地视觉核验截图属于开发机临时产物，未随项目发布，以避免把本地服务器信息带入仓库。
- 参考图尺寸：1440 × 1024 px
- 实现截图视口：约 1265 × 711 px，浏览器设备像素比为 1；由于视口高度不同，本次以布局关系、信息层级、对比度和溢出情况为准，不作逐像素比较。

## 已核验状态

- 中文首页：运行中的任务、搜索、梦境筛选、服务器/PID 添加监视、最近活动、梦境信息。
- 首页侧栏“梦境”：不跳转配置页，直接切换到所有已保存服务器的 GPU 概览；每台服务器显示系统、GPU 数量、型号、显存和利用率。
- 梦境概览按服务器从上到下排列，每个服务器使用可折叠卡片，每个 GPU 使用自适应小卡片显示显存、利用率、温度和功耗进度条，并以 5 秒间隔刷新快照。
- 梦境概览中的服务器卡片可折叠；收起状态右侧圆形标志显示空闲 GPU 数量。
- 梦境页只显示 GPU 信息；点击侧栏具体服务器后切换到对应服务器详情。
- 服务器详情采用左右双栏：左侧为 GPU 卡片，右侧为 GPU 运行进程表，显示 GPU、PID、用户、GPU 显存和命令；窄屏时自动上下排列。
- 侧栏的上方工作区导航与下方服务器导航互斥选中；进入服务器详情时只保留下方服务器的选中状态。
- 首页侧栏“连接器”：不跳转配置页，直接切换到连接器信息概览；显示飞书状态、智能体、App ID、API 地址、传输方式和已发现会话。
- 中文连接器页：左侧“连接器”选中，飞书连接器状态、App ID、保密字段、消息发现和保存/测试入口可见。
- 英文切换：首页标题可切换为 `Running tasks`，语言按钮可切回中文。
- 监视入口：未填写 PID 时，添加按钮保持禁用；同一运行中服务器/PID 由前端和后端共同防重。
- 数据接口：`/api/monitors` 返回 200；当前页面只呈现 `RUNNING` 任务，不保留已结束任务。
- 自动刷新：页面保持局部更新，输入/配置区域不会因刷新重建而抖动或清空。
- 响应式布局：当前浏览器尺寸下工具栏保持在页面范围内，表格和右侧梦境信息不产生横向溢出。

## 设计决策

- 采用石墨黑底、青绿色强调色和固定信息列，突出运行状态与可操作控件。
- 将“飞书配置”并入通用“连接器”配置分区，后续可继续增加其他通知连接器。
- 首页只保留运行中任务，已完成或取消任务不进入任务列表，降低监视噪声。
- 配置页使用左侧分区选择、中央内容和右侧提示，避免多个设置文档同时堆叠。
- 保留现有 SSH、VSCode 导入和 Feishu 功能；本轮重点调整信息架构和视觉布局，没有引入新的图片资源依赖。
